{% extends 'base.html.twig' %}

{% block title %}Joboffer{% endblock %}

{% block body %}
    <div class="card text-center">
        <img class="be-img-home" src="{{ asset('build/images/villeAnnonce.jpg') }}" alt="">
        {#  <img class="be-img-home" src="{{ asset('build/images/StatsWork2.png') }}" alt=""><div class="card-body">
            <h5 class="card-title">{{ joboffer.contract.type }}</h5>
            <p class="card-text">{{ joboffer.job.name}}</p>
            <p class="card-text">{{ joboffer.city }} / {{ joboffer.updatedAt ? joboffer.updatedAt|date('Y-m-d H:i:s') : '' }} </p>
            <a href="#" class="be-primary-button">Postuler</a>
        </div>
        <div class="card-footer text-muted" style="width: 18rem;">
            <p class="card-text">{{ joboffer.company.name }}</p>
        </div>#}
    </div>
    <h1>Joboffer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ joboffer.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ joboffer.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ joboffer.description }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ joboffer.city }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ joboffer.createdAt ? joboffer.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ joboffer.updatedAt ? joboffer.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Company</th>
                <td>{{ joboffer.company.name }}</td>
            </tr>
            <tr>
                <th>Job</th>
                <td>{{ joboffer.job.name}}</td>
            </tr>
            <tr>
                <th>Contract</th>
                <td>{{ joboffer.contract.type }}</td>
            </tr>
            <tr>
                <th>Salary : min / max</th>
                <td>{{ joboffer.salary.min }} / {{ joboffer.salary.max }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_joboffer_index') }}">back to list</a>

    <a href="{{ path('app_joboffer_edit', {'id': joboffer.id}) }}">edit</a>

    {{ include('joboffer/_delete_form.html.twig') }}
{% endblock %}
