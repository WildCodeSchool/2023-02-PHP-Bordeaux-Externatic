<form method="post" action="{{ path('app_joboffer_delete', {'id': joboffer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ joboffer.id) }}">
    <button class="be-primary-button btn">Delete</button>
</form>
