{{ form_start(form) }}
    {{ form_errors(form) }}
<div class="col-md-12 px-4">
    {{ form_row(form.name) }}
</div>
<div class="col-md-12 px-4">
    {{ form_row(form.pathFile) }}
</div>
<div class="text-center mb-4">
    <button class="be-primary-button" type="submit">Envoyer</button>
</div>
{{ form_end(form) }}

