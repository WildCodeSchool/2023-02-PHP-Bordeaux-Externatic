{% extends 'base.html.twig' %}

{% block title %}Salaire {{ salary.id }}{% endblock %}

{% block body %}
    <h1>Salaire {{ salary.id }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salary.id }}</td>
            </tr>
            <tr>
                <th>Min</th>
                <td>{{ salary.min }}</td>
            </tr>
            <tr>
                <th>Max</th>
                <td>{{ salary.max }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_salary_index') }}">Retour à la liste</a>

    <a href="{{ path('app_salary_edit', {'id': salary.id}) }}">Modifier</a>

    {{ include('salary/_delete_form.html.twig') }}
{% endblock %}
