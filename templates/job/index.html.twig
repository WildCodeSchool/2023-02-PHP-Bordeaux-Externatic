{% extends 'base.html.twig' %}

{% block title %}Emploi {{ job.id }}{% endblock %}

{% block body %}
    <h1>Emploi {{ job.id }}</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for job in jobs %}
            <tr>
                <td>{{ job.id }}</td>
                <td>{{ job.name }}</td>
                <td>
                    <a href="{{ path('app_job_show', {'id': job.id}) }}">Voir</a>
                    <a href="{{ path('app_job_edit', {'id': job.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_job_new') }}">Créer un nouveau</a>
{% endblock %}
