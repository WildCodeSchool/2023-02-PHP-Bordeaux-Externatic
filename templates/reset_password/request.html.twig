{% extends 'base.html.twig' %}

{% block title %}Reset your password{% endblock %}

{% block body %}
    {% for flash_error in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}

    {{ form_start(requestForm, {'attr': {'class': 'row g-3 be-form my-6'}}) }}
    <h1 class="text-center">Réinitialiser votre mot de passe </h1>

            <div class="my-4 col-md-6">
                {{ form_row(requestForm.email, {'attr': {'class': 'form-control p-0 be-form-input col-10', 'placeholder':
                    'Entrez votre adresse mail...', 'aria-label': 'Search', 'name': 'what',
                }}) }}
            </div>
        <div class="d-flex justify-content-center">
            <button class="be-primary-button mb-4">Envoyer</button>
        </div>
    {{ form_end(requestForm) }}
{% endblock %}
