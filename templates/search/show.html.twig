{% extends 'base.html.twig' %}

{% block title %}Recherche {{ search.id }}{% endblock %}

{% block body %}
    <h1>Recherche {{ search.id }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ search.id }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ search.city }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_search_index') }}">Retour à la liste</a>

    <a href="{{ path('app_search_edit', {'id': search.id}) }}">Modifier</a>

    {{ include('search/_delete_form.html.twig') }}
{% endblock %}
